# 検証記録

## 0.2.1：人の交代と操作メニュー

2026-09-21、Node 21テスト・ビルド成功。実ブラウザーで以下を確認しました。コントローラー入力はキーボード型モードと通常のキーボードで検証しています。

- 問題2・1手・クリア2問の状態から「次の人へ」を開くと、消去対象の説明が表示され、「交代しない」が初期選択されること。
- キャンセル後、問題番号・盤面・手数・クリア記録が維持されること。
- Bでひもの選択を解除し、もう一度Bでメニューを開けること。上下で「はじめから」を選び、Aで確認画面を開き、コントローラーだけでやり直せること。問題2・0手となり、クリア2問は維持されること。
- 1手進めた状態から、コントローラーだけで「次の人へ」→確認→実行し、問題1・0手・クリア0問・Undo不可になること。文字拡大とコントローラー操作中の状態は維持されること。
- 交代後もBでメニューを開き、使用不可のUndoを飛ばして移動できること。Aでヒントを表示できること。
- 再読込後も問題1・0手・クリア0問・文字拡大が保存されていること。
- 選択中のメニュー項目に枠が表示され、盤面下にもコントローラー操作案内が表示されること。JavaScriptエラー0件。
- PR #3の26テスト・ビルドとPages配信が成功。公開URLの19ファイルがHTTP 200で取得でき、ローカル配信物のSHA-256とすべて一致。公開画面で更新→交代確認→キャンセルを確認し、リリース `v0.2.1` を公開。

## 0.2.0：工作コントローラーの接続補助

2026-09-21、Windows / Node.js 24.19.0、標準Python、Chromiumブラウザーで検証。

- `cd web && npm test`：21件成功。既存9件に加え、USBパケットの厳密な解析、分割受信・長すぎる行からの復帰、中央・ボタン解放待ち、方向のしきい値とリピート、A/Bの押下エッジ、中央調整・反転、Gamepadの標準／非標準入力、シリアル接続・DTR・切断・再接続・許可取消・遅れた許可応答の無効化を確認。
- `python -m unittest discover -s raspberry_pi/usb -p 'test_*.py'`：5件成功。CircuitPythonモジュールを模擬し、GPIO値からの送信形式、押したとき1になること、分割書き込み、未接続時、USBデータ有効化の起動設定を確認。
- `npm run build`：19ファイルを出力。USB用コード・手順を含む全アセットをService Workerでキャッシュ。サブディレクトリでのオフライン取得は自動テストでも確認。
- 初期画面に接続ボタンが表示され、接続ダイアログから方式を選択できること。Pico用の準備・配線・保存リンクを確認。
- キーボード型のテストで上下左右・A/Bの6項目が確認済みになり、テスト中はパズルが0手のまま保持されること。
- その入力経路で問題1を1手でクリアし、Bメニュー→スティック選択→AでUndoできること。
- クリア直後のAが次の問題へ持ち越されず、手を離した後のAで問題2へ進むこと。
- 前版から更新ボタンで切り替え、問題・クリア記録が維持されること。再読込で操作モードは停止し、勝手に接続・ゲーム操作を再開しないこと。
- 配信サーバーを停止した状態で、ゲームを開き直して接続テスト→クリア→次の問題へ進めること。USB用 `boot.py` / `code.py` / `README.txt` の全3ファイルもブラウザーで開けること。
- 1366×900および1024×768、文字拡大での接続画面を確認。ブラウザーのJavaScriptエラーログは0件。
- PR #2をマージし、GitHub Actionsの26テスト・ビルド・Pages配信が成功。公開URLの19ファイルがHTTP 200で取得でき、SHA-256が検証済みローカル配信物とすべて一致。
- 公開URLで旧版から更新ボタンを押し、新しい接続画面・Pico方式の案内を確認。JavaScriptエラー0件。リリース `v0.2.0` にPWA配信ZIPとPico用USBコード・手順ZIPを添付。

**実物のPico / Pico W、USBゲームパッド、工作キーボード機器では未検証**です。上記は模擬通信と実ブラウザーの通常キーボードを用いた検証です。実機ではポート認識、6入力、中央調整と反転、ケーブル抜き差し、再接続、オフライン再接続を確認してください。

## 0.1.0：初版の記録

2026-09-21、Windows / Node.js 24.19.0、Codex内のChromiumブラウザーで検証。

## 自動テスト

`cd web && npm test`：9件成功。

- 20問すべての盤面の妥当性、初期状態で全てのひもが絡まっていること、保存した解答手順だけで合法的に完全クリアできること。
- 通常の交差、線の重なり、端と線内部の接触、離れた線の判定。
- 非隣接・使用済み・範囲外への移動拒否と、元の盤面が変わらないこと。
- 問題1・5・10・15・20で、初期解答から外れた合法手の後も探索結果を再生してクリアできること。
- 探索上限に到達した場合は「解答未発見」を返すこと。
- 保存、復元、Undo履歴、破損データ、容量制限の処理。
- インストール用マニフェストとPNGの実サイズ。
- 全キャッシュ対象ファイルの存在、サブディレクトリでのオフライン画面・Worker読込。
- Service Workerが他アプリのキャッシュやPOST等の通信を変更しないこと。

`npm run build`：成功、外部依存なしの13ファイルを出力。内容ハッシュからService Workerのキャッシュ版を作成。

## ブラウザーで確認した操作

- 通常画面、1366×900、1024×768のPC幅。大きな文字での表示。
- 問題1でヒント→クリック→クリア→１手戻す。
- 問題1でEnter選択→左矢印のキーボード操作だけでクリア。
- 問題一覧から問題20に移動、クリア記録表示。
- 新しいビルドを配置後、更新通知→更新ボタン→新版への切り替え。
- 問題20で2手進め、うち1手を既定の解答と異なる手に変更。その後、ローカル配信サーバーを停止。
- サーバー停止中にページを再読込し、問題番号・盤面・2手の履歴を復元。
- サーバー停止中に新しいWeb Workerを起動し、現在の盤面からヒントを取得。
- そのヒントをたどり、問題20を計11手でクリア。
- 当該ブラウザーのJavaScriptエラーログは0件。

配信サーバー停止による確認は「配信元が利用できない状態」の検証です。OS全体のネット接続設定は変更していません。

## 公開環境

2026-09-21、GitHub Pagesの公開リポジトリ・標準runnerを使って配信しました。

- 公開URL: https://31916.ch/himoparty/
- https://31916.github.io/himoparty/ から上記URLに転送されることを確認。既存のアカウント側ドメイン設定によるものです。
- GitHub Actionsのテスト・ビルド・Pages配信が成功。
- HTTPSを強制。HTML、CSS、全JavaScript、マニフェスト、全アイコンの13ファイルがHTTP 200で取得でき、SHA-256が検証済みのローカル配信物と一致。
- JavaScriptとマニフェストのContent-Typeを確認。
- 公開URLを実ブラウザーで開き、初期盤面、ヒント表示、Service Workerのキャッシュ完了表示を確認。JavaScriptエラー0件。

## 未検証

Chrome/EdgeのOSへの実インストール、別PC、スクリーンリーダーによる読み上げ、小学生・高齢者本人による使い勝手。
